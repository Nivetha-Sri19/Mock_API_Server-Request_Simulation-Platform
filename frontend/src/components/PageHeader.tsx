import { Box, Typography } from "@mui/material";
export default function PageHeader({eyebrow,title,subtitle,action}:{eyebrow?:string;title:string;subtitle?:string;action?:React.ReactNode}){return <Box sx={{mb:3.5,display:"flex",alignItems:"flex-end",justifyContent:"space-between",gap:2,flexWrap:"wrap"}}><Box>{eyebrow&&<Typography sx={{color:"primary.main",fontSize:10,letterSpacing:".18em",fontWeight:900,mb:1}}>{eyebrow}</Typography>}<Typography variant="h3" sx={{fontSize:{xs:30,md:42}}}>{title}</Typography>{subtitle&&<Typography sx={{color:"text.secondary",mt:1,maxWidth:760}}>{subtitle}</Typography>}</Box>{action}</Box>}
